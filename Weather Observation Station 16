# HackerRank_SQL
#Solution of HackerRank SQL(Prepare by Topic)
#Query the smallest Northern Latitude (LAT_N) from STATION that is greater than 38.7780. Round your answer to  decimal places.
#######

(SELECT ROUND(MIN(T.LAT_N),4) FROM (select LAT_N FROM STATION WHERE LAT_N>38.7780) T);
